# JWT secret for authentication (change in production)
JWT_SECRET=your-secret-key-change-in-production

# OpenAI API key for recipe parsing (required for AI fallback when JSON-LD is not found)
OPENAI_API_KEY=sk-...
